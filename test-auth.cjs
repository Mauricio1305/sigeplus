const http = require('http');

async function test() {
  const loginData = JSON.stringify({ email: 'suport.mp@gmail.com', password: 'password' }); // We don't have password, let's create a temp user maybe? No, let's bypass auth if we could.
}
test();
